import React from 'react'
import CodeBlock from '../components/CodeBlock'

function AddToCart3() {


  return (

    <div>
        <p>Add to cart with 'select size', 'select color', 'select design' and pass the value to cart </p>
        <p>User can add multiple items even with the exactly the same item in the cart, to do this, add a unique id to the order, and add the unique id to the order on the 'remove' button like as an id </p>
      <CodeBlock >
        {` 
       `}
      </CodeBlock>



    </div>
  )
}

export default AddToCart3